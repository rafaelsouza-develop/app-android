<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.app4pets.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App4PetsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="br.com.app4pets.app.presentation.home.HomeActivity"
            android:label="@string/title_activity_home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="br.com.app4pets.app.presentation.auth.login.LoginActivity" />
        <activity android:name="br.com.app4pets.app.presentation.auth.forgotpassword.ForgotPasswordActivity" />
        <activity android:name="br.com.app4pets.app.presentation.auth.register.RegisterActivity" />
        <activity android:name="br.com.app4pets.app.presentation.pet.petcreate.PetCreateActivity" />
        <activity
            android:name="br.com.app4pets.app.presentation.pet.petprofile.PetProfileActivity"
            android:label="@string/title_activity_pet_profile"
            android:theme="@style/AppTheme.NoActionBar" />

        <service android:name=".util.notification.FirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>